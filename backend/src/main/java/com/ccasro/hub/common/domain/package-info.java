/**
 * Shared domain components. Contains reusable value objects, domain primitives, and domain
 * utilities used across multiple bounded contexts. This layer must remain framework-independent.
 */
package com.ccasro.hub.common.domain;
